# Backend (Python)
backend/.venv/
backend/__pycache__/
backend/**/__pycache__/
backend/*.db
backend/.env
backend/.pytest_cache/

# Frontend (Next.js)
frontend/node_modules/
frontend/.next/
frontend/.env.local
frontend/next-env.d.ts
# frontend/AGENTS.md and frontend/CLAUDE.md are regenerated by `next dev` on every run but are
# meant to be committed (see the note inside frontend/AGENTS.md) so they stay tracked, not ignored.

# OS / editor
.DS_Store
