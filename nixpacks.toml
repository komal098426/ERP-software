[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["python -m pip install --upgrade pip", "pip install -r backend/requirements.txt || pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "python run.py"
